import { ZonaRefriValidators } from './zona-refri-validators';

describe('ZonaRefriValidators', () => {
  it('should create an instance', () => {
    expect(new ZonaRefriValidators()).toBeTruthy();
  });
});
